close all; clear; clc;
addpath(genpath('../function/'));

% if any(size(dir('./output/*.mat'),1))
%     movefile './output/*.mat' './output/mat/'
%     % delete('./output/*.mat');
% end

%% ------------------------ Parameters --------------------------------------------
isGPU = 0;

% maxit = 200;       % Max iterations

%% fix the random seed (for reproductibility)
rng(1);
useGPU(isGPU);

data_dir = './output/mat/';
file_name = 'LS';


solve_lst = dir([data_dir, file_name, '*.mat']);  run('PlotCmp.m');

